name: Survey123 Action

on:
  webhook:
    types:
      - survey123

jobs:
  process_survey_data:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Process Survey Data
        run: |
          echo "Received Survey Data: ${{ github.event.payload.data }}"
          # Add your code here to process the data